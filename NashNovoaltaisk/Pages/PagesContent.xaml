<Page
    x:Class="NashNovoaltaisk03.Pages.PagesContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:control="using:NashNovoaltaisk03.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NashNovoaltaisk03.Models"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="NewsItemTemplate" x:DataType="models:NewsData">
            <control:NewsItemControl />
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <GridView
            x:Name="MainPageListView"
            Background="#FFECECEC"
            ItemsSource="{x:Bind Mpvm.PageContent}"
            AutomationProperties.AutomationId="ItemGridView"
            AutomationProperties.Name="Grouped Items"
            ItemTemplate="{StaticResource NewsItemTemplate}"
            SelectionMode="None"
            IsSwipeEnabled="false"
            IsItemClickEnabled="True" />
    </Grid>
</Page>